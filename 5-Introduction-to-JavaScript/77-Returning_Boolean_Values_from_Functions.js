// Using the Comparison with the Equality Operator all comparison operators return a boolean true or false value. This will eliminate the need to use if/else statement to do a comparison.

function isLess(a, b) {
  // Fix this code
  return a <= b;
}

// Change these values to test
isLess(10, 15);
