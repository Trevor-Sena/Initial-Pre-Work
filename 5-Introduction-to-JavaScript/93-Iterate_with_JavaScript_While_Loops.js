// You can run the same code multiple times by using  a "while" loop hich runs "while" a specified condition is true and stops once that condition is no longer true.

// Setup
var myArray = [];

// Only change code below this line.
var i = 0;
while(i <5) {
  myArray.push(i);
  i++;
}
