// Inline functions don't need to be named because we do not reuse them anywhere else. When there is no function body, and only a return value, arrow function syntax allows you to omit the keyword return as well as the brackets surrounding the code.

const magic = () => new Date();
